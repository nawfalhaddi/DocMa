const initialState = {
    id: null,
    firstName: null,
    lastName: null,
    accessCode: null,
    alertes: []
}

export default (state = initialState, actions) => {
    switch (actions.type) {

        default:
            return state;
    }
}